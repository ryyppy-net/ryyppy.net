version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build started on `date`
      - mvn package
artifacts:
  files:
    - target/ryyppynet.war
    - appspec.yml
    - scripts
  discard-paths: yes