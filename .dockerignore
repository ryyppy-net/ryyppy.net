# The image builds the jar itself (see the Dockerfile's Maven stage), so the
# build context needs pom.xml and src/ - but never a local target/, whose
# stale output would otherwise invalidate the build cache on every rebuild.
target/
.git/
e2e/
docker/
scripts/
*.md
